// Import the existing Plugin App component
import PluginApp from '../../../pages/plugins/App';

/**
 * Plugins 页面包装器
 * 集成现有的 Plugin 管理组件到新的路由系统中
 */
const Plugins: React.FC = () => {
  return <PluginApp />;
};

export default Plugins;
